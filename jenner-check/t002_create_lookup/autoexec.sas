/* jenner-check bundle autoexec: cap rows. The %create_lookup macro is defined  */
/* in script.sas, adapted from inputfiles/macros/create_lookup.sas — the only   */
/* change is the target library (lookup -> work) so it runs without an external */
/* libname assignment.                                                          */
options obs=100;
